subprojects {
    apply plugin: 'java'

    repositories {
        mavenLocal()
        mavenCentral()
    }
}

ext.pluginsDir = rootProject.buildDir.path + '/../app/build/libs/plugins'
task build(dependsOn: [':app:uberjar'])
